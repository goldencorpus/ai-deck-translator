# Decktr worker — additive deps on top of the engine's requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2
cryptography>=42  # AES-256-GCM decrypt of the customer Google refresh token (token_crypto.py)
